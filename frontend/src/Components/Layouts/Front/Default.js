const Default = (props) => {

    return (
        <>
            <section className="body-sign">
                {props.children}
            </section>

        </>
    );
}

export default Default;